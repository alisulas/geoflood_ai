
  # GeoFloodAI BTN

  This is a code bundle for GeoFloodAI Bank BTN, a flood prediction for property buyer so the user can buy the property that safe, zero flood risk.
  

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  